/// Named `NSEvent` virtual key codes used by the app, to avoid bare magic
/// numbers (スタイルガイド.md §2).
enum KeyCode {
    /// Escape — closes the expanded notch (要件定義.md §5.3 補助).
    static let escape: UInt16 = 53
}
